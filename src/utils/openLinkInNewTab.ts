export default function openLinkInNewTab() {
  const elements = document.getElementsByTagName('a')
  console.log(elements)
  //   console.log(elements[0]?.setAttribute('target', '_blank'))
  //   console.log(Array.from(elements))

  // for (let element of elements) {
  //   let status = elements.    ('data-status')
  //   if (status === 'open') {
  //     // Grab the data
  //   }
  // }
  //         window.open(elements.href, '_blank')
}
