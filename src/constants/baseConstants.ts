/* eslint-disable import/prefer-default-export */
import {
  DRAFT_LABEL,
  INBOX_LABEL,
  JUNO_BASE_LABEL,
  SENT_LABEL,
  SPAM_LABEL,
  TODO_LABEL_NAME,
  TRASH_LABEL,
} from './globalConstants'

export const BASE_ARRAY = [
  JUNO_BASE_LABEL,
  TODO_LABEL_NAME,
  INBOX_LABEL,
  SPAM_LABEL,
  DRAFT_LABEL,
  SENT_LABEL,
  TRASH_LABEL,
]
